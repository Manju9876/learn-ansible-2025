- name: vault retrive
  hosts: localhost
  connection: local
  tasks:
    - name: print the secret
      ansible.builtin.debug:
        msg: "{{lookup('community.hashi_vault.hashi_vault', 'secret=roboshop-dev-secrets/data/frontend token={{ vault_token }}  url=http://vault.devopsbymanju.shop:8200') }}"

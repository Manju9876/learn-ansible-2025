- name: vault retrive
  hosts: localhost
  connection: local
  tasks:
    - name: print the secret
      ansible.builtin.debug:
        msg: "{{ lookup('community.hashi_vault.hashi_vault', 'secret=roboshop-dev-secrets/data/catalogue:MONGO_URL token={{ vault_token }}  url=http://vault.devopsbymanju:8200') }}"

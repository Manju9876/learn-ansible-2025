- name: retrive the vault secrets
  hosts: localhost
  connection: local
  become: yes
  tasks:
    - name: read the password
      ansible.builtin.debug:
        msg: "{{ lookup('community.hashi_vault.hashi_vault', 'secret=test/data/demo:value token={{ pass_token }} url=http://vault.devopsbymanju.shop:8200') }}"
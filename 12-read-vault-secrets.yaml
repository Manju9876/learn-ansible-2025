- name: retrive the vault secrets
  hosts: localhost
  connection: local
  become: yes
  tasks:
    - name: read the vault
      ansible_module_debug:
        msg:  "{{ lookup('community.hashi_vault.hashi_vault', 'secret=test/data/username:value token={{ vault_token }} url=http://vault.devopsbymanju.shop:8200') }}"
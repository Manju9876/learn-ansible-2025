- name: retrive the vault secrets
  hosts: localhost
  connection: local
  become: yes
  tasks:
    - name: read the password
      ansible.builtin.debug:
        msg: "{{ lookup('community.hashi_vault.hashi_vault', 'secret=test/data/demo token={{ pass_token }} url=http://vault.devopsmanju.shop:8200') }}"